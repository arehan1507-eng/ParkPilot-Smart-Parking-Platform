# Dataset

The training script downloads the Cleveland subset of the UCI Heart Disease dataset automatically.

Run:

```bash
python train.py
```

The raw dataset is intentionally not duplicated here. Check the dataset's original UCI terms and attribution requirements before redistributing it in a repository.
